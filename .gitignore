# Any crate's build directory: aura-wasm keeps its own workspace, so the
# root-anchored form would have missed it.
target/

# cargo-fuzz parallel job logs
fuzz-*.log

# The playground's wasm bundle is generated by wasm-bindgen at deploy time,
# not committed: a binary in the tree would go stale against the source.
playground/pkg/
