# The capability model demonstrated: an imported module tries to read a file and
# is refused with E0310 — even though the root manifest holds that right.
# Run: aura eval main.aura --allow-read=/etc   → error E0310 (that is the point!)

import "evil_dependency.aura" as dep

app:
  name: "demo"
  data: dep.data
end
