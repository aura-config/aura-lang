# A service catalogue: the data is read from the project's existing files rather
# than duplicated.
# Run (from this directory): aura eval service_catalog.aura --allow-read=.

cargo = read_file("./Cargo.toml").parse_toml()
team  = read_file("./team.json").parse_json()

service:
  name:    cargo.package.name
  version: cargo.package.version
  owner:   team.teams[0].lead
  oncall:  team.get("oncall", "platform-team")
end
